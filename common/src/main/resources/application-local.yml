spring:
  main:
    allow-bean-definition-overriding: true
  profiles: local
  profiles.include: default
  mvc:
    converters:
      preferred-json-mapper: gson

  datasource:
    platform: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/stock?autoReconnect=true
    username: root
    password: sprtms1225
    initial-size: 5
    min-idle: 3
    max-active: 10
    test-while-idle: true
    connection-test-query: SELECT 1
    hikari:
      connection-test-query: SELECT 1
  jpa:
    show-sql: true
    format-sql: true
    open-in-view: false
    hibernate:
      use-sql-comments: true
      use-new-id-generator-mappings: true
      ddl-auto: validate
      naming:
        physical-strategy: org.springframework.boot.orm.jpa.hibernate.SpringPhysicalNamingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format-sql: true

redis:
  news:
    port: 6379
    host: 127.0.0.1
    password: redis-cli
    timeout: 300

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE